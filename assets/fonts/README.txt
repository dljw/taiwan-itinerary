Optional offline font files. The site works without them — it falls back to
PingFang / Songti / system mono via fonts.css.

If you want locked-in faces, drop:
  TripMono.woff2
  TripSans.woff2
  TripSerif.woff2
into this folder (subsetted Noto / IBM Plex work well).
